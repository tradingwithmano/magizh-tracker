# Copy this file to .env for local development
# Never commit .env to git

# Server port (default: 3000)
PORT=3000

# Database path
# Local:  leave empty (defaults to ./data/tracker.db)
# Render: /data/tracker.db  (set via render.yaml)
DB_PATH=
